# tic-tac-toe-score-tracker-301751-301763

## Frontend

See `tic_tac_toe_frontend/README.md` for setup, required env vars, and Supabase schema.

This app reads Supabase configuration from build-time `NG_APP_SUPABASE_URL` and `NG_APP_SUPABASE_KEY` (injected by the platform / `.env`).